name: Test

on:
  workflow_dispatch:

jobs:
  build::
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v3
      - name: Add value to .env file
        id: time
        run: touch .env
        
    
